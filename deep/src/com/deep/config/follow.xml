<?xml version="1.0" encoding="UTF-8" ?>

<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "mybatis-3-mapper.dtd">
			
<mapper namespace="follow">

<!-- FollowContorller.getFollower() -->
<select id="getFollower" parameterType="int" resultType="com.deep.model.domain.Follow">
	SELECT *  FROM deep_follow
	WHERE deepFollower=#{deepFollower}
</select>


<!-- FollowContorller.getFollowing() -->
<select id="getFollowing" parameterType="int" resultType="com.deep.model.domain.Follow" >
	SELECT *	FROM deep_follow
	WHERE deepFollowing=#{deepFollowing}
</select>

<!-- FollowContorller.addFollow() -->
<insert id="addFollow" parameterType="int" >
	INSERT INTO deep_follow(deepFollower, deepFollowing)
	VALUES(#{DeepFollower}, #{DeepFollowing})
</insert>

<!-- FollowContorller.setFollow() -->
<delete id="setFollow" parameterType="int" >
	DELETE FROM deep_follow
	WHERE deepFollowing =#{DeepFollowing}
</delete>

</mapper>