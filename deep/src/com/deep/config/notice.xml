<?xml version="1.0" encoding="UTF-8" ?>

<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "mybatis-3-mapper.dtd">
			
<mapper namespace="notice">
	
	<update id = "setNotice"  parameterType="int" >
		UPDATE deep_notice SET deepNoticeStatus = 2
		WHERE deepNoticeNo = #{memberNo}
	</update>
	
	<insert id = "addNotice"  parameterType="int" >
		INSERT INTO deep_notice(deepNoticeNo, deepNoticeCategory, deepMemberNo, deepNoticeTarget1, deepNoticeTarget2, deepNoticeStatus)
		VALUES (#{NoticeNo}, #{NoticeCategory}, #{NoticeTarget1}, #{NoticeTarget2}, #{inputNoticeStatus})
	</insert>
</mapper>